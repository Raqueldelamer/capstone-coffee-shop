import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "LOGIN"
  }
};

export const Search = {
  args: {
    label: "PRODUCTS"
  }
};

export const Exit = {
  args: {
    label: "CART"
  }
};