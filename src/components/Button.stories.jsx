import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Purchase"
  }
};

export const Search = {
  args: {
    label: "Search"
  }
};

export const Exit = {
  args: {
    label: "Exit"
  }
};