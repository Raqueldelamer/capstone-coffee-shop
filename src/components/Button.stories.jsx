import Button from './Button';

const meta = {
  component: Button,
};

export default meta;

export const Default = {
  args: {
    label: "Sign Up"
  }
};

export const Login = {
  args: {
    label: "Login"
  }
};

export const CART = {
  args: {
    label: "ADD to CART"
  }
};