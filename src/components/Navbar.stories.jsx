import Navbar from './Navbar';

const meta = {
  component: Navbar,
};

export default meta;

export const Default = {
  args: {
    menuItems: ["HOME", "ABOUT", "LOGIN", "PRODUCT"],
    title: "Coffee Shop"
  }
};