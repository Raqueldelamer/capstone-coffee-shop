import Navbar from './Navbar';

const meta = {
  component: Navbar,
};

export default meta;

export const Default = {
  args: {
    menuItems: ["HOME", "ABOUT", "PURCHASE"],
    title: "Navbar"
  }
};